/**
 * The `send-only` tag: uppercase 10px mono on the signal well, the one job that ground has. It
 * marks an address with no inbox wherever addresses are listed — the From switch, Settings.
 */
export const SendOnlyTag = () => (
  <span className="shrink-0 bg-signal-well px-1 py-px font-mono text-[10px] tracking-[0.08em] text-signal uppercase">
    send-only
  </span>
);
